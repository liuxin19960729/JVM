## 12.3.2 内存间交互操作
```
主内存 ->工作内存(Java 线程) 是怎么交互的

note:
    JAVA 内存模型定义了8种操作来交互并且这些操作都是原子贷(long double 内存在某些平台上并不是原子的)

1.lock
    作用于主内存,把该变量标识未该线程独占
2.unlock
    作用于主内存,使变量的锁定状态释放
3.read
    作用于主内存,主内存的值传输到工作内存
4.load
    
``` 